public class dateClass{
    public void show(){
   date dat = date.today();
    date da = date.today().adddays(-3);
        system.debug('time'+date.Today());
        system.debug('time'+date.Today().year());
        system.debug('day'+da );
    }
   
}