<apex:page controller="testClassViewState">
<apex:form >
    <apex:pageBlock >
        <apex:pageBlockTable value="{!attachmentList}" var="att">
            <apex:column value="{!att.id}"/>
            <apex:column value="{!att.name}"/>
            <apex:column > 
                <!-- <apex:image value="{!att.body}"/>     -->      
            </apex:column>
        </apex:pageBlockTable>
    </apex:pageBlock>
</apex:form>
</apex:page>