<apex:page controller="aggrigatequeryDemo">
     <apex:pageBlock >
         <apex:pageblockTable value="{!models}" var="con">
             <apex:column value="{!con.count}"/>
         </apex:pageblockTable>
     </apex:pageBlock>
</apex:page>