<apex:page controller="JoinQueryControllerpractice">
    <apex:pageBlock >
        <apex:pageblockTable value="{!record}" var="con">
        <apex:column value="{!con.lastname}"/>
        <apex:column value="{!con.account.name}"/>
        </apex:pageblockTable>
    </apex:pageBlock>
</apex:page>