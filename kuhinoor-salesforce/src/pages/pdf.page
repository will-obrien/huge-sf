<apex:page standardController="Booking__c" recordSetVar="con" renderAs="PDF">
 
     <apex:pageBlock >
         <apex:pageblocktable value="{!con}" var="c">
             <apex:column value="{!c.Amount__c}"/>
               
         </apex:pageblocktable>
     </apex:pageBlock>
</apex:page>