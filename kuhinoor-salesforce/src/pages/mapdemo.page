<apex:page controller="MapDemoController">
    <apex:pageBlock >
        <apex:form >
            <apex:selectList size="1">
                <apex:selectOptions value="{! statevalue}" />    
            </apex:selectList>
            <apex:selectList size="1">
                <apex:selectOptions value="{! states}" />    
            </apex:selectList>
        </apex:form>
    </apex:pageBlock>
</apex:page>