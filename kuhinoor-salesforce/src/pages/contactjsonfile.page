<apex:page standardController="contact" recordSetVar="contacts" contentType="json">
<apex:pageBlock >
    <apex:pageBlockTable value="{!contacts}" var="con">
        <apex:column value="{!con.firstName}"/>
    </apex:pageBlockTable>
</apex:pageBlock>
</apex:page>