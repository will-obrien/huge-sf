<apex:page controller="joinQueryDemoController">
     <apex:pageBlock >
         <apex:pageblocktable value="{!contacts}" var="con">
         <apex:column value="{!con.firstname}" />
         <apex:column value="{!con.lastname}"/>
         <apex:column value="{!con.account.name}"/>
         </apex:pageblocktable>
     </apex:pageBlock>
</apex:page>