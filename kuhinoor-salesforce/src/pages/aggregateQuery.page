<apex:page controller="aggregateQueryController">
     <apex:pageBlock >
         <apex:pageblocktable value="{!contacts}" var="con">
         <apex:column value="{!con.contactCount}"/>
         
         </apex:pageblocktable>
     </apex:pageBlock>
</apex:page>