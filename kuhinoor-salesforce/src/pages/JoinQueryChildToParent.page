<apex:page controller="JoinQueryChildToParentController">
    <apex:pageBlock >
        <apex:pageBlockTable value="{!childToParentforContactToAccount}" var="con">
            <apex:column value="{!con.firstname}"/>
            <apex:column value="{!con.lastname}"/>
            <apex:column value="{!con.Account.name}">
            </apex:column>
        </apex:pageBlockTable>
    </apex:pageBlock>
</apex:page>