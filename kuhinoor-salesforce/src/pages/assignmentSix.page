<apex:page controller="assignmentSixController" action="{!upsertcon}">
     <apex:pageBlock >
         <apex:pageBlockTable value="{!contactModels}" var="con">
         <apex:column value="{!con.firstname}"/>
         <apex:column value="{!con.lastname}"/>
         
         <apex:column value="{!con.phone}"/>
         <apex:column value="{!con.email}"/>
         <apex:column value="{!con.account}"/>
         </apex:pageBlockTable>
    </apex:pageBlock>
</apex:page>