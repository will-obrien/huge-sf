<apex:page showHeader="true" sidebar="true" standardController="zqu__Quote__c" >

	<!-- The progress bar should be rendered for the quote wizard -->
	<c:ProgressBar />

	<!-- Render the payment page component, using the url parameters as attribute values -->
	<c:PaymentPage zuoraPageId="{!$CurrentPage.parameters.pageId}" submitEnabled="{!$CurrentPage.parameters.submitEnabled}" style="{!$CurrentPage.parameters.style}" />
	
</apex:page>