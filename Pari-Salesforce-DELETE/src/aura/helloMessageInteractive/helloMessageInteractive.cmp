<aura:component >
    <aura:attribute name="message" type="String"/>
    <p>Message of the day: {!v.message}</p>
    
    
    <div>
    	<lightning:button variant="brand" label="You look nice today" onclick="{! c.handleClick }" />
    	<lightning:button variant="brand" label="Today is going to be a great day" onclick="{! c.handleClick }" />
    </div>
	
</aura:component>