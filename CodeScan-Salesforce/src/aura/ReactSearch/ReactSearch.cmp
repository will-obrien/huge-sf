<aura:component controller="ContactController" implements="flexipage:availableForAllPageTypes">
    <ltng:require scripts="{!$Resource.ReactSearch + '/bundle.js'}"
                  afterScriptsLoaded="{!c.reactInit}"
                  />
     
    yyy<div aura:id="app"/>yyy
</aura:component>