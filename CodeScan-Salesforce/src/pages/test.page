<apex:page showHeader="false" controller="foo">

          <script type="text/javascript"> 
           if (this.state.numbersOnly)  {
           !
          $Organization.Id
          }

                         if (this.state.numbersOnly) {
                    ! $Organization.Id /^[0-9]+([,.][0-9]+)?$/g.test(e) && e.indexOf(".") != e.length - 1 || (e.match(RegExp("\\.", "g")) || []).length > 1 ? (this.setState({
                        error: !0
                    }), n = !1) : this.props.error || this.setState({
                        error: !1
                    });
                }
 </script>
 
 
 <apex:form >
 
 </apex:form>
 

</apex:page>
